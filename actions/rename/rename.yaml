runtime: plugin
action:
  title: Rename
  description: Rename section and update playbooks
  arguments:
    - name: old
      title: Old Path
      description: Current section path
      required: true
    - name: new
      title: New Path
      description: New section path
      required: true
