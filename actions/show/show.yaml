runtime: plugin
action:
  title: Show
  description: Show allocations and attachments
  arguments:
    - name: section
      title: Section
      description: Chassis section path (optional, shows all if omitted)
  options:
    - name: platform
      shorthand: p
      title: Platform
      description: Filter nodes by platform instance
      type: string
      default: ""
