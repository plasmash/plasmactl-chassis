runtime: plugin
action:
  title: Show
  description: Show details for a chassis section
  arguments:
    - name: section
      title: Section
      description: Chassis section path (e.g., platform.foundation.cluster.control)
      required: true
  options:
    - name: platform
      shorthand: p
      title: Platform
      description: "Filter nodes by platform instance (default: all)"
      type: string
      default: ""
